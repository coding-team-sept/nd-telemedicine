server:
  port: 9002

spring:
  application:
    name: chat

  datasource:
    username: 'admin'
    url: jdbc:postgresql://localhost:5432/telemedic_chat
    password: 'admin'

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
    hibernate:
      ddl-auto: update
    show-sql: 'true'

